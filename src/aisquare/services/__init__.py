"""Service layer — real behaviour lives here, one module per domain.

Every public function currently raises the shared stub from
``aisquare.core.stubs``. To implement a feature, replace the ``stub(...)``
call with real logic; the signature and return type are already final.
"""
